import 'package:flutter/material.dart';

class CheckBarangPage extends StatelessWidget {
  const CheckBarangPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Check Barang'),
    );
  }
}
